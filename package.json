{
  "name": "design-system-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "preview:frontend": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "sync:figma": "tsx tools/sync-figma.ts",
    "copy:knowledge": "node tools/copy-knowledge.mjs",
    "build": "npm run copy:knowledge && npm run build:frontend",
    "deploy:pages": "npm run copy:knowledge && npm --prefix frontend run build"
  },
  "dependencies": {
    "@vercel/node": "^5.6.18",
    "openai": "^4.29.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "dotenv": "^16.4.5",
    "tsx": "^4.7.3",
    "typescript": "^5.4.5"
  }
}
